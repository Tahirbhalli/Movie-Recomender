export function setMovieImages(state, movieImages) {
  state.movieimages = movieImages;
}
export function setMoviesList(state, allmovies) {
  state.allmovies = allmovies;
}
export function settokken(state, tokken) {
  state.tokken = tokken;
}
export function setstar(state, star) {
  state.star = star;
}